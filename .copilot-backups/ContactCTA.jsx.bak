import {
  ArrowRight,
  Mail,
  MessageCircle,
  Phone,
} from "lucide-react";

function ContactCTA() {
  return (
    <section className="contact-cta" id="contact">
      <div className="container contact-cta-container">
        <div className="contact-copy">
          <p className="section-label">Private introductions</p>

          <h2>Let’s curate your next collection.</h2>

          <p>
            Contact our team for bespoke sourcing, private viewings and tailored
            gemstone advisory designed for discerning collectors.
          </p>
        </div>

        <div className="contact-actions">
          <a href="https://wa.me/" target="_blank" rel="noreferrer">
            <MessageCircle size={20} />
            <span>Connect via WhatsApp</span>
            <ArrowRight size={18} />
          </a>

          <a href="tel:+94000000000">
            <Phone size={20} />
            <span>Request a Call</span>
            <ArrowRight size={18} />
          </a>

          <a href="mailto:info@surangagems.com">
            <Mail size={20} />
            <span>Send an Inquiry</span>
            <ArrowRight size={18} />
          </a>
        </div>
      </div>
    </section>
  );
}

export default ContactCTA;